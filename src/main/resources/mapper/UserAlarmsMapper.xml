<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.edog.mapper.UserAlarmsMapper">

    <!-- 获取用户所有启用的闹钟按时间排序 -->
    <select id="selectUserAlarmsByUserId" resultType="com.example.edog.entity.UserAlarms">
        SELECT * 
        FROM user_alarms 
        WHERE user_id = #{userId} 
            AND status = 1 
        ORDER BY alarm_time ASC
    </select>

    <!-- 获取指定时间范围内用户的所有闹钟 -->
    <select id="selectAlarmsByTimeRange" resultType="com.example.edog.entity.UserAlarms">
        SELECT * 
        FROM user_alarms 
        WHERE user_id = #{userId} 
            AND status = 1 
            AND alarm_time &gt;= #{startTime} 
            AND alarm_time &lt;= #{endTime} 
        ORDER BY alarm_time ASC
    </select>

    <!-- 检查重复闹钟 -->
    <select id="selectDuplicateAlarm" resultType="com.example.edog.entity.UserAlarms">
        SELECT * 
        FROM user_alarms 
        WHERE user_id = #{userId} 
          AND alarm_time = #{alarmTime}
          AND type = #{type}
          AND status = 1
          <if test="type == 1">
            AND target_date = #{targetDate}
          </if>
        LIMIT 1
    </select>

</mapper>
