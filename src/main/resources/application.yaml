spring:
  application:
    name: LAMP
  datasource:
    url: jdbc:mysql://sh-cynosdbmysql-grp-mtd4hmc8.sql.tencentcdb.com:28669/lamp_health?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: Mimawangle2
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 300000
      connection-timeout: 20000
      pool-name: HikariCP

server:
  port: 6060
  # Tomcat WebSocket 缓冲区配置
  tomcat:
    max-http-form-post-size: 10MB
    connection-timeout: 60000

# 扣子智能体配置
kouzi:
  agent:
    base-url: https://api.coze.cn/v3/chat
    token: sat_LeDY8iu23Ifcb2UwY7LXfZeL0HhoF4NTswQmlooFVJyRJNd7ExEk9gFogjnRPbPl
    bot-id: "7589593616806068233"

# 阿里云占位配置（开发环境可使用占位，请在生产环境使用真实凭证）
aliyun:
  appKey: ***
  accessKeyId: ***
  accessKeySecret: ***

# MyBatis-Plus 配置
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # SQL日志输出
    map-underscore-to-camel-case: true  # 下划线转驼峰
  global-config:
    db-config:
      id-type: auto  # 主键自增
      logic-delete-field: deleted  # 逻辑删除字段
      logic-delete-value: 1  # 逻辑已删除值
      logic-not-delete-value: 0  # 逻辑未删除值

