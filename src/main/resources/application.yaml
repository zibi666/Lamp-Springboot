spring:
  application:
    name: LAMP
  datasource:
    url: jdbc:mysql://sh-cynosdbmysql-grp-mtd4hmc8.sql.tencentcdb.com:28669/EDOG?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: Mimawangle2
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 300000
      connection-timeout: 20000
      pool-name: HikariCP

server:
  port: 6060

# 扣子智能体配置
kouzi:
  agent:
    base-url: https://api.coze.cn/v3/chat
    token: ******
    bot-id: "7589593616806068233"

# 阿里云占位配置（开发环境可使用占位，请在生产环境使用真实凭证）
aliyun:
  appKey: ******
  accessKeyId: ******
  accessKeySecret: ******
